# Release Notes

## POS Enhancements
- **Feature**: Added new `ExpenseCategoriesController` and `TaxCategoriesController` to support frontend dropdowns and mappings.
- **Enhancement**: Configured `CrudDataTable` to conditionally render action buttons based on form fields availability, used for read-only `SalesPage`.
- **Feature**: Added POS payment flow modal with `Tendered Amount` and `Change (Kembalian)` calculation.
- **Enhancement**: Centralized string translations using `i18next`.
- **UI**: Updated `AdminLayout` to group navigation items into Inventory, People, Operations, and Settings.
